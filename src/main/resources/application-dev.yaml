
server:
    port: 8080

spring:
    application:
        name: mybatis-plus
    datasource:
        username: root
        password: root
#        url: jdbc:mysql://localhost:3306/uyun_udap?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8&useSSL=true
#        driver-class-name: com.mysql.cj.jdbc.Driver
        #性能分析工具的配置
        driver-class-name: com.p6spy.engine.spy.P6SpyDriver
        url: jdbc:p6spy:mysql://127.0.0.1:3306/uyun_udap?characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8

#mybatis:
#    mapper-locations: classpath:mapping/*.xml
#    type-aliases-package: com.example.demo.bean
#showSql
#logging:
#    level:
#        com.example.mapper: debug

#mybatis日志
mybatis-plus:
    global-config:
        db-config:
            logic-delete-value: 1
            logic-not-delete-value: 0
            id-type: auto
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
    level:
        root: warn
        com.kaven.mybatisplus.dao: trace
    pattern:
        console: '%p%m%n'